{
  "name": "i-am-one-with-the-force",
  "version": "0.1.0",
  "private": true,
  "author": {
    "name": "Simon Relet",
    "email": "simon.relet@murex.com"
  },
  "engines": {
    "node": ">=8.9.4",
    "npm": ">=5.6.0",
    "yarn": ">=1.3.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/simonrelet/i-am-one-with-the-force/"
  },
  "scripts": {
    "build": "react-scripts build",
    "clean": "rm -rf build coverage",
    "format": "prettier --write \"**/*.{js.css,md}\"",
    "lint": "yarn lint-js && yarn lint-format",
    "lint-js": "eslint .",
    "lint-format": "prettier --list-different \"**/*.{js.css,md}\"",
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "devDependencies": {
    "eslint": "^4.16.0",
    "prettier": "^1.10.2"
  }
}
